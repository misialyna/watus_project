# --- Core / STT / I/O ---
numpy==1.26.4
sounddevice==0.4.6
soundfile==0.12.1
webrtcvad==2.0.10
faster-whisper==1.0.3

# --- Messaging / HTTP / config ---
pyzmq==25.1.2
requests==2.32.3
python-dotenv==1.0.1

# --- Reporter (health, mini API) ---
fastapi==0.112.2
uvicorn==0.30.6

# --- Speaker verification (ECAPA) ---
speechbrain==0.5.16
